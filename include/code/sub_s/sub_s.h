#ifndef Z3DS_HUD_CONTROLLER_SUB_S_STUB_H
#define Z3DS_HUD_CONTROLLER_SUB_S_STUB_H

// The upstream decomp tree generates this header for full-game builds. This
// HUD-only code mod does not use those declarations, but global.h includes it
// through z64subs.h, so provide an empty shim for standalone NRM builds.

#endif
